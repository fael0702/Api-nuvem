import { Sku } from "../../entities/Sku";

export default class SkuController {
    public async create(sku: Sku) {

        try {
            
        } catch (error) {
            console.error(error)
        }
    }
}